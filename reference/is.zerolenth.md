# Check If Input Has Zero Length

Check If Input Has Zero Length

## Usage

``` r
is.zerolenth(x)
```

## Arguments

- x:

  input data: a vector, list, or array

## Value

whether `x` has zero length
