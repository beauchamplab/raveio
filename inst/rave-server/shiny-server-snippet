server {
  listen ${port};
  location / {
    app_dir "${root_dir}/app/main";
    log_dir "${root_dir}/log";
    bookmark_state_dir "${root_dir}/bookmarks";
    directory_index on;

    location /preprocess {
      app_dir "${root_dir}/app/preprocess";
    }

  }

}
